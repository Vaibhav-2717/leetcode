class Solution {
    public boolean search(int[] nums, int target) {
        int flag=0;
        for(int i=0;i<nums.length;i++){
            if(nums[i]==target){
                flag=1;
            }
        }
       
         if(flag==1)
            return true;
            else
            return false;
    }
}
